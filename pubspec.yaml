name: someapp
description: Sample Flutter project for firebase.

publish_to: "none"

version: 1.0.0

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^0.5.1
  firebase_auth: ^0.18.2
  flutter_riverpod: ^0.12.1
  cloud_firestore: ^0.14.2
  cloud_functions: ^0.7.0-dev.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
